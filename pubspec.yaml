name: be_rich_app
description: "A solução de todos os problemas financeiros."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.3.0
  firebase_auth: ^4.7.2
  cloud_firestore: ^4.2.0
  google_sign_in: ^6.0.0

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  fl_chart: 0.68.0
  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
