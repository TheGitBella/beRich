import 'package:flutter/material.dart';

class TabInvestimentos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Conteúdo da Aba Investimentos'),
    );
  }
}
