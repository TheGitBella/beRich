import 'package:flutter/material.dart';

class TabMetas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Conteúdo da Aba Metas'),
    );
  }
}
