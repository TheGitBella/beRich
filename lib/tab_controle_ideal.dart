import 'package:flutter/material.dart';

class TabControleIdeal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Conteúdo da Aba Controle Ideal'),
    );
  }
}
